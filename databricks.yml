bundle:
  name: bigdata-etl

workspace:
  root_path: /Shared/${bundle.name}

include:
  - resources/*.yml

# 4 Declaración correcta de variables (como mapas con default)
targets:
  dev:
    workspace:
      root_path: /Shared/Github/dev  # override solo para dev
  prod:
    workspace:
      root_path: /Shared/Github/prod